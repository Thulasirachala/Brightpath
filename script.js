document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('requestForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));localStorage.setItem('brightpath_last_request',JSON.stringify(data));document.getElementById('formMessage').textContent='✓ Request submitted successfully in prototype mode!';form.reset();});}});function demoLogin(){const msg=document.getElementById('loginMessage');const email=document.getElementById('loginEmail').value;if(!email){msg.textContent='Please enter your email.';return}msg.textContent='Demo login successful. Use the dashboard buttons below.';}